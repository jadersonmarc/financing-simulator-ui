/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['cdn.motor1.com'], // Adicione outros domínios conforme necessário
  },
};

export default nextConfig;
